zip กับ forkJoin ใช้สำหรับ เวลาเราอยาก call api หลายตัวพร้อมกัน แล้วรอให้ api ทุกตัวได้ผลลัพธ์ก่อน แล้วเอาผลลัพธ์ทุกอย่างมารวมกัน

combineLatest ตัวอย่างการใช้ เช่น มี filter 2 ตัว เป็นเพศ กับ อายุ ถ้าเราเปลี่ยนค่าตัวนึง มันก็จะเปลี่ยนค่าเป็นค่าล่าสุด แล้วค่อย combine ส่งไปที่ api อะไรก็ว่าไป